#include "song.h"
#include "allegro.h"

/*
SAMPLE *playloop(char pas){
    SAMPLE sample=load_sample(pas);
    if (sample == NULL)
    {
        allegro_message("pas pu trouver le fichier son %s",pas);
        exit(EXIT_FAILURE);
    }
    play_sample(sample, 100, 0, 1000, 1);//1 loop
    return sample;
}
 */